<p>
	<%= image_tag @pin.image.url(:medium) %>
  <strong>Description:</strong>
  <%= @pin.description %>
</p>
<% if @pin.user == current_user %>
	<%= link_to 'Editar', edit_pin_path(@pin) %>
<% end %>
<%= link_to 'Voltar', pins_path %>
